---
import ProductDetailPage from '../../../components/pages/ProductDetailPage.astro';

// Store is in "coming soon" mode — demo product detail pages are not generated.
// Restore products.map(...) here to re-enable real product pages.
export function getStaticPaths() {
  return [];
}

const { product } = Astro.props;
---
<ProductDetailPage locale="es" product={product} />
